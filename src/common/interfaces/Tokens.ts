export interface ITokens {
  address: string;
  blockNumber: number;
  blockHash: string;
  balance: string;
  name: string;
  symbol: string;
  decimals: number;
  contract: string;
  contract_interface: string[];
}
